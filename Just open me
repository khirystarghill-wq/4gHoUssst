<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>a haiku a day</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital@0;1&family=Orbitron:wght@400;600&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg1:#07040b;
      --bg2:#12071a;
      --ink:#f4e9ff;
      --muted: rgba(244,233,255,.72);
      --veil: rgba(255,255,255,.08);
      --veil2: rgba(255,255,255,.04);
      --glow: rgba(255,170,230,.18);
      --accent: #ff86c8;
      --accent2:#bba2ff;
    }

    *{ box-sizing:border-box; }
    html,body{ height:100%; }
    body{
      margin:0;
      color:var(--ink);
      background:
        radial-gradient(900px 600px at 18% 20%, rgba(255,134,200,.12), transparent 55%),
        radial-gradient(900px 600px at 78% 35%, rgba(187,162,255,.11), transparent 55%),
        radial-gradient(1200px 900px at 50% 120%, rgba(255,255,255,.04), transparent 60%),
        linear-gradient(180deg, var(--bg1), var(--bg2));
      overflow-x:hidden;
      font-family: "Playfair Display", serif;
    }

    /* subtle film grain */
    body:before{
      content:"";
      position:fixed; inset:0;
      background-image:
        radial-gradient(circle at 20% 10%, rgba(255,255,255,.05) 0 1px, transparent 1px),
        radial-gradient(circle at 80% 30%, rgba(255,255,255,.04) 0 1px, transparent 1px),
        radial-gradient(circle at 40% 70%, rgba(255,255,255,.03) 0 1px, transparent 1px);
      background-size: 180px 180px, 210px 210px, 240px 240px;
      opacity:.35;
      pointer-events:none;
      mix-blend-mode: screen;
    }

    .wrap{
      min-height:100%;
      display:flex;
      align-items:center;
      justify-content:center;
      padding: 60px 18px 80px;
      position:relative;
      isolation:isolate;
    }

    .card{
      width:min(720px, 100%);
      padding: 26px 22px 20px;
      border:1px solid rgba(255,255,255,.12);
      background: linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
      box-shadow: 0 20px 90px rgba(0,0,0,.55);
      border-radius: 18px;
      backdrop-filter: blur(10px);
      position:relative;
      z-index:2;
    }

    h1{
      margin: 0 0 12px;
      font-family: "Orbitron", system-ui, sans-serif;
      font-weight:600;
      letter-spacing:.06em;
      text-transform: lowercase;
      font-size: 20px;
      opacity:.95;
    }

    .sub{
      margin:0 0 18px;
      color:var(--muted);
      font-style: italic;
      line-height:1.35;
    }

    .haiku{
      white-space:pre-line;
      font-size: 22px;
      line-height: 1.55;
      padding: 14px 14px;
      border-radius: 14px;
      background: linear-gradient(180deg, var(--veil), var(--veil2));
      border: 1px solid rgba(255,255,255,.10);
      box-shadow: inset 0 0 0 1px rgba(0,0,0,.15);
      margin: 14px 0 14px;
    }

    .metaRow{
      display:flex;
      flex-wrap:wrap;
      gap:10px;
      align-items:center;
      justify-content:space-between;
      margin-top: 10px;
      color:var(--muted);
      font-family: "Orbitron", system-ui, sans-serif;
      font-size: 12px;
      letter-spacing:.05em;
    }

    .pill{
      padding: 8px 10px;
      border: 1px solid rgba(255,255,255,.12);
      border-radius: 999px;
      background: rgba(0,0,0,.20);
    }

    .sign{
      margin-top: 18px;
      border-top: 1px dashed rgba(255,255,255,.18);
      padding-top: 14px;
      display:flex;
      justify-content:flex-end;
      font-family: "Orbitron", system-ui, sans-serif;
      letter-spacing:.18em;
      color: rgba(255,255,255,.86);
      text-transform: lowercase;
    }

    /* ---------- GHOSTS ---------- */
    .ghost{
      position:absolute;
      width: 150px;
      height: 200px;
      z-index:1;
      filter: drop-shadow(0 22px 30px rgba(0,0,0,.55));
      opacity:.9;
      pointer-events:none;
    }

    /* main body */
    .ghost .sheet{
      position:absolute;
      inset:0;
      background:
        radial-gradient(40px 40px at 45% 30%, rgba(0,0,0,.22) 0 2px, transparent 3px),
        radial-gradient(40px 40px at 62% 30%, rgba(0,0,0,.22) 0 2px, transparent 3px),
        radial-gradient(80px 50px at 52% 25%, rgba(0,0,0,.10), transparent 60%),
        linear-gradient(180deg, rgba(255,255,255,.92), rgba(255,255,255,.65));
      border-radius: 70px 70px 52px 52px;
      transform-origin: 50% 10%;
      overflow:hidden;
      border: 1px solid rgba(255,255,255,.28);
    }

    /* drape folds */
    .ghost .sheet:before{
      content:"";
      position:absolute;
      inset:-10px -10px 0 -10px;
      background:
        radial-gradient(120px 180px at 35% 55%, rgba(0,0,0,.10), transparent 60%),
        radial-gradient(120px 180px at 70% 52%, rgba(0,0,0,.10), transparent 62%),
        repeating-linear-gradient(90deg,
          rgba(0,0,0,.05) 0 6px,
          transparent 6px 18px);
      opacity:.35;
      mix-blend-mode:multiply;
      transform: skewX(-2deg);
    }

    /* bottom scallops */
    .ghost .sheet:after{
      content:"";
      position:absolute;
      left:-6px; right:-6px; bottom:-18px;
      height: 50px;
      background:
        radial-gradient(circle at 10% 0%, rgba(255,255,255,.70) 0 26px, transparent 27px),
        radial-gradient(circle at 30% 0%, rgba(255,255,255,.70) 0 26px, transparent 27px),
        radial-gradient(circle at 50% 0%, rgba(255,255,255,.70) 0 26px, transparent 27px),
        radial-gradient(circle at 70% 0%, rgba(255,255,255,.70) 0 26px, transparent 27px),
        radial-gradient(circle at 90% 0%, rgba(255,255,255,.70) 0 26px, transparent 27px);
      filter: blur(.2px);
      opacity:.9;
    }

    /* blush aura */
   
